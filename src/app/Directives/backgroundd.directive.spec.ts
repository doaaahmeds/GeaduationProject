import { BackgrounddDirective } from './backgroundd.directive';

describe('BackgrounddDirective', () => {
  it('should create an instance', () => {
    const directive = new BackgrounddDirective();
    expect(directive).toBeTruthy();
  });
});
